# Copy to .env and fill in a strong random secret (32+ chars).
# Both the API and the shell (or shell-sim) MUST share the same value.
# NEVER commit .env or the actual secret.
#
# Generate one with: [System.Web.Security.Membership]::GeneratePassword(48,0)
# or: openssl rand -base64 36
#
JWT_SECRET=8vK2rNf7QxP4mT9zYcL6wHs3JdEa5UgB1XyR8kMz0NpV7qCt4FwS2hJn9DbL6eGx

# Production note (see PROOF.md): move to RS256 + JWKS endpoint published by the shell.
# That removes the shared-secret requirement entirely.
